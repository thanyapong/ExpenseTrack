import { Typography } from "@mui/material";

const SearchAlertTyphography = () => {

    return (
        <Typography sx={{ color: "red" }}>
            กรุณากด "ค้นหา" เพื่อแสดงข้อมูล
        </Typography>
    );
};

export default SearchAlertTyphography;
